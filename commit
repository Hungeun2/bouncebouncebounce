#!/usr/bin/env bash
set -euo pipefail

if [[ $# -lt 2 || "$1" != "-m" ]]; then
  echo "Usage: commit -m \"message\""
  exit 1
fi

message="$2"
shift 2

if [[ $# -gt 0 ]]; then
  echo "Usage: commit -m \"message\""
  exit 1
fi

git add .
git commit -m "$message"
git push origin main
